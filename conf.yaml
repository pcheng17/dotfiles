- defaults:
    link:
        relink: true

- clean: ['~']

- create:
    - ~/.vim/undodir

- shell:
    - command: sh -c 'curl -fLo $HOME/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim'
      description: Installing vim-plug for vim
      quiet: true
    - command: sh -c 'curl -fLo $HOME/.local/share/nvim/site/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim'
      description: Installing vim-plug for neovim
      quiet: true

- link:
    ~/.gitconfig: .gitconfig
    ~/.tmux.conf: .tmux.conf
    ~/.config/nvim/init.vim:
        create: true
        path: nvim/init.vim
    ~/.config/nvim/lua:
        create: true
        path: nvim/lua
    ~/.vim/my-snippets:
        create: true
        path: nvim/snippets
    ~/.vim/after: nvim/after
